# E-Migration Assist

E-Migration Assist helps users navigate the immigration process by providing a 5-step assessment, optional document uploads, and WhatsApp integration, while offering an administrative CRM for lead management.

## Run & Operate

*   **Install dependencies:** `pnpm install`
*   **Run development server:** `pnpm dev`
*   **Build:** `pnpm build`
*   **Typecheck:** `pnpm typecheck`
*   **Generate OpenAPI client:** `pnpm orval`
*   **DB Push:** `pnpm db:push` (DrizzleKit migrations)

**Required Environment Variables:**
*   `DATABASE_URL`: PostgreSQL connection string
*   `ADMIN_EMAIL_TOKEN`: Legacy operator-only `x-admin-token` fallback (kept for back-compat with scripts)
*   `RESEND_API_KEY`: Resend API key for email sending
*   `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_PHONE_NUMBER`: Twilio credentials for WhatsApp
*   `OTP_SECRET`: Secret for OTP hashing
*   `REPLIT_OBJECT_STORAGE_URL`: Replit Object Storage endpoint
*   `BOOTSTRAP_ADMIN_EMAIL`, `BOOTSTRAP_ADMIN_PASSWORD` (optional): override the seeded demo admin (defaults: `demo@admin.local` / `ChangeMe!2026`)
*   `PUBLIC_BASE_URL` (optional): public origin used to build password-reset links (falls back to `https://$REPLIT_DEV_DOMAIN`)

## Stack

*   **Frameworks:** Express 5 (backend), React (frontend)
*   **Runtime:** Node.js 24, TypeScript 5.9
*   **ORM:** Drizzle ORM
*   **Validation:** Zod
*   **Build Tool:** esbuild
*   **Database:** PostgreSQL

## Where things live

*   **Backend API Routes:** `api-server/src/routes/`
*   **Database Schema:** `api-server/src/db/schema.ts`
*   **OpenAPI Specification:** `openapi.yaml`
*   **Frontend Pages:** `artifacts/emigration-assist/src/pages/`
*   **Frontend Components:** `artifacts/emigration-assist/src/components/`
*   **Shared Utilities:** `api-server/src/lib/`, `artifacts/emigration-assist/src/lib/`
*   **CSS/Theming:** `artifacts/emigration-assist/src/index.css`
*   **Country Data:** `artifacts/emigration-assist/src/lib/countries.ts`

## Architecture decisions

*   **Bidirectional Lead Pipeline (Phase 5 §10):** Lead statuses are now bidirectional — operators may drag/select forward OR backward across the funnel. The single remaining hard invariant is the `converted` predecessor lock: entering `converted` still requires the current status to be `ready_for_case` (or already `converted`), because the same PATCH triggers `ensureCaseForLead` and case creation must remain a deliberate handover. Moving back OUT of `converted` is permitted; the linked `lead_cases` row is left in place (benign data artefact). Case statuses remain forward-only — only the lead funnel was relaxed. Server-side enforcement lives in the PATCH `/api/admin/leads/:id` route's WHERE-clause predicate (kept atomic to close TOCTOU); UI mirror lives in `lib/leadStatus.ts → canAdvanceStatus`.
*   **Idempotent Lead-to-Case Conversion:** Converting a lead to a case is an idempotent operation, ensuring no duplicate cases are created upon repeated attempts.
*   **OTP Verification with Fallback:** OTP requests prioritize WhatsApp, falling back to email on provider failure, and include a development code for non-production environments.
*   **Optimistic UI with Server-Side Validation:** Admin CRM features use optimistic UI updates for responsiveness, with server-side re-validation and rollback for critical actions like status changes.
*   **Minimal PII in Public APIs:** Public endpoints for status lookup generalize information and avoid revealing Personally Identifiable Information, complemented by robust enumeration defense and rate limiting.
*   **Pre-Traffic Hardening (V3.1):** WhatsApp webhook is fail-closed — 503 if neither `WHATSAPP_APP_SECRET` nor `TWILIO_AUTH_TOKEN` is set; 401 if the `X-Twilio-Signature` header is missing or fails HMAC verification (no inbound message is persisted). `POST /api/leads` enforces a per-hour sliding-window limit (10/IP, 5/email, 5/canonical-WA) before zod parsing, plus a `website` honeypot field that returns a synthetic 201 without writing. Every privileged admin mutation (status / priority / notes / conversion / case status / outbound message attempt / document download / manual-contact click) writes a row to `lead_audit` via `lib/audit.ts`; the actor credential is sha256-hashed (cookie session id OR x-admin-token) and the raw value is never stored. Frontend-only events use `POST /api/admin/audit` (allow-list of action names) so browser-side actions like tel:/mailto: clicks still land in the trail.
*   **Lead Conversion Predecessor Lock (V3.1):** The `converted` terminal status may only be reached from `ready_for_case` (or no-op'd from `converted` itself). Any earlier status → converted is rejected with HTTP 409. Enforced atomically inside the UPDATE's WHERE clause to close the TOCTOU race.
*   **Two-Phase Lead Submission (V3):** The lead row is committed at end of step 6 (Terms) with `finalize:false` (no email/WhatsApp dispatched); the confirmation is sent only when `POST /api/leads/:id/finalize` is called at the very end of the flow. The finalize route is at-most-once: it skips dispatch if any `lead_engagements` row of type `confirmation` already exists for the lead, bounding the abuse surface of the unauthenticated endpoint.
*   **Dynamic Assessment Step Count:** The assessment is 7 steps when the user opts out of supporting documents, 8 when they opt in. The Yes/No "do you have supporting documents to upload?" gate sits between Terms and Upload/Summary.
*   **Session-Scoped Document Listing:** `DocumentUploader` accepts a `sessionStartedAt` cutoff and filters out documents created before that moment, so a returning user (same email/whatsapp → dedup match on the existing lead) never sees documents from a previous session.
*   **Inside/Outside-SA Residence Logic:** When the user picks "outside South Africa" the residence dropdown excludes ZA via `excludeIso`. When they pick "inside" with an empty residence, ZA is auto-selected; when they switch from a ZA residence back to "outside", ZA is cleared.
*   **Admin Auth (V3 — email + password):** `admin_users`, `admin_sessions`, `admin_password_resets` tables in `lib/db/src/schema/admin.ts`. Login mints an opaque session id stored server-side and set as the httpOnly `ema_admin_session` cookie (7-day TTL, sameSite=lax, secure in prod). The shared `requireAdminAuth` / `requireAdminToken` middleware checks the cookie first and falls back to the legacy `x-admin-token` header so operator scripts keep working. On startup, if `admin_users` is empty, a demo admin (`demo@admin.local` / `ChangeMe!2026`, override via `BOOTSTRAP_ADMIN_EMAIL`/`BOOTSTRAP_ADMIN_PASSWORD`) is seeded as superadmin and the credentials are logged at WARN. Forgot-password mints a 1-hour single-use token (sha256-hashed in DB) emailed via Resend; "Manage Admins" (`/admin/users`) is superadmin-only and includes a self-protection guard so a superadmin can't disable/demote/delete themselves.

## Product

*   Public-facing 5-step assessment for lead capture.
*   Optional document upload for leads (PDF, JPG, PNG, DOC, DOCX).
*   WhatsApp integration for lead contact and OTP verification.
*   Public status lookup using a reference number.
*   Email engagement system for confirmations and updates.
*   Lightweight administrative CRM for lead and case management.
*   Lead engagement tracking for outbound communication.
*   Case lifecycle management with forward-only status progression.
*   Inbound WhatsApp message processing via webhooks with intent detection.
*   OTP verification for lead creation.

## User preferences

I prefer concise and accurate responses.
I like to work iteratively, meaning I prefer to discuss changes and review them in stages rather than having a large change implemented all at once.
Please ask for confirmation before making significant changes to the codebase or architectural decisions.
Ensure all solutions are robust and consider edge cases.

## Gotchas

*   **WhatsApp Webhook Now Fail-Closed:** Reverses the prior always-200-to-Twilio strategy. Missing/invalid signatures now return 401 and Twilio will retry per its backoff schedule. Set `WHATSAPP_APP_SECRET` (preferred) or keep `TWILIO_AUTH_TOKEN` set; absence of both yields 503.
*   **Lead Honeypot Field:** The public form must NEVER render an input named `website`; the API silently rejects any submission whose body includes a non-empty `website` string (returns a synthetic 201 with id `00000000-…` and reference `EMA-PENDING-OK`). New form fields must avoid that name.
*   **Document Deletion Scope:** `DELETE /api/documents/:id` requires `leadId` scope and will reject requests without it.
*   **Legal Modals:** All legal modals currently display placeholder copy awaiting final review.
*   **Orphaned Blobs:** Object storage blobs are not deleted when documents are removed (V1 limitation).
*   **Autofill:** Chromium occasionally autofills WhatsApp input despite attempts to defeat it; handled by clearing during tests.
*   **`POST /api/leads` `finalize` Flag:** Defaults to `true` for back-compat. The web frontend always passes `finalize:false` from the assessment so the confirmation isn't sent until `/finalize` is hit. Any new caller must remember to pass `finalize:false` if it intends to defer the dispatch.
*   **Legacy `adminToken.ts` Shim:** `getAdminToken()` now returns the placeholder string `"cookie-auth"` so the many existing fetch sites keep their `if (!token) return;` guards green and the (irrelevant) header is harmless server-side. `clearAdminToken()` redirects to `/admin/login`. Real auth is the session cookie. New admin fetches should use `credentials: "include"` and skip the token helper entirely.
*   **Step 7 Schema Optionality:** `wantsToUploadDocuments` is `.optional()` in the assessment zod schema even though the UI requires it. This is so that step 6's `type=submit` Continue button isn't silently blocked by zod-resolver before the user has even seen the gate. The Submit/Finalize buttons themselves enforce the choice via `disabled={!wantsDocs}`.
*   **CRM Phase A — Foundations (admin-only, no UI surfaces yet):** Extended the admin CRM data model to support the SaaS dual-lead architecture. `prelaunch_leads` gained: `lead_type` (NOT NULL DEFAULT 'individual'; discriminator `individual` vs `professional`), `inquiry_type` (`visa_inquiry` | `overstay_appeal` | `travel_entry_assistance`; meaningful for individuals only), `source` (DEFAULT 'web_form'), `assigned_to` (uuid → admin_users.id, no FK constraint enforced — checked at API layer), `last_contacted_at`, `next_follow_up_at`, `tags` (text[]), and 10 professional-lead columns (`organization_name`, `organization_type`, `representative_*`, `website`, `firm_size`, `operating_regions` text[], `service_focus`, `estimated_client_volume`). `admin_users` gained an explicit `role` column (`superadmin` | `admin` | `sales` | `operations` | `viewer`, NOT NULL DEFAULT 'admin') that coexists with `is_superadmin` — the boolean stays authoritative for `gateSuperadmin()` until Phase E lands finer RBAC. `lead_audit` gained `actor_user_id` (nullable uuid) which `writeAudit` now populates from `req.adminUser?.id` when session-cookie auth was used; legacy x-admin-token callers still leave it null by design. The status enum extended from 7 → 10 (`new → reviewing → contacted → awaiting_response → engaged → qualified → proposal_sent → ready_for_case → converted → closed`); the three new statuses were inserted in funnel-monotonic positions so every previously-valid `canAdvanceStatus` transition stays valid. Priority gained `critical` above `high`. Backfill: 28 historical leads → all `individual`, with `inquiry_type` derived from `immigration_situation` (`overstay` → `overstay_appeal`, else `visa_inquiry`); 2 admins → 1 `superadmin` + 1 `admin` from the existing boolean. **The public POST /api/leads insert path was not touched** — new columns rely on DB defaults / NULL, so the assessment flow contract is unchanged. OpenAPI Lead schema extended with all new fields; `leadType` is in `required` since the column is NOT NULL. Two new sibling schemas codified what the non-`Lead`-shaped endpoints actually return so the contract is honest end-to-end: `AdminLeadListItem` for `GET /api/leads` (slim dashboard row, omits rules-engine fields, surfaces leadType + inquiryType + source + assignedTo + lastContactedAt + nextFollowUpAt + tags) and `PublicLead` for `GET /api/leads/{referenceNumber}` (public-safe subset, no leadStatus/leadPriority/PII). The full `Lead` schema is still the contract for the admin detail endpoints (`POST /leads`, `GET /leads/by-id/:id`).
*   **Phase 4 Campaign Engine (one-shot bulk):** New tables `campaigns`, `campaign_recipients`, `unsubscribes` in `lib/db/src/schema/campaigns.ts`. Server-side: `lib/audienceQuery.ts` (zod-validated single-level AND/OR query compiler over a 12-field whitelist on `prelaunch_leads`, 32-rule cap), `lib/whatsappCampaign.ts` (24h-window detector via `case_messages` inbound; template path is a documented V1 stub returning `wa_template_send_not_implemented`), `lib/unsubscribe.ts` (HMAC-signed `(channel, contact)` token; SECRET fails closed in production if `UNSUBSCRIBE_SECRET`/`SESSION_SECRET` unset), `lib/campaignRender.ts` (4 merge tokens: `{{first_name}} {{full_name}} {{reference}} {{organization_name}}`). `routes/adminCampaigns.ts` exposes list / create / get / patch(draft) / delete(draft) / preview / test / send under `/api/admin/campaigns`. `routes/unsubscribe.ts` exposes public `GET/POST /api/unsubscribe` with RFC-8058 one-click semantics. **Send route invariants:** atomic draft→sending claim (concurrent click 409s); 200-recipient hard cap; pre-flight refusals revert to draft; per-recipient failures stay non-fatal; the post-claim loop is wrapped in try/catch so any uncaught exception still finalises the row to `cancelled` (never stuck in `sending`); email sends are refused with 500 if no `PUBLIC_BASE_URL`/`REPLIT_DEV_DOMAIN` is set so the unsubscribe footer is always present. Frontend: `/admin/campaigns` (list), `/admin/campaigns/:id/edit` (editor with channel-locked draft, audience-query-builder component, debounced auto-save, live preview, test send, confirm-to-N send dialog), `/admin/campaigns/:id` (detail with stat cards + recipients table). Channel is chosen at create-time via dialog (email or whatsapp) and is immutable afterwards. **Known V1 limitation:** WhatsApp out-of-window sends require a Twilio Content template; the dispatcher currently returns `wa_template_send_not_implemented` so those recipients land as `failed` until the Content API call is wired in. In-window WA freeform and all email sends are fully functional.
*   **Phase 5 Phase A — CRM polish (no schema change):** (1) Renamed table column "Visa Type" → "Type of Enquiry" with a new derivation in `lib/typeOfEnquiry.ts` mapping existing `leadType` + `inquiryType` + `immigrationSituation` (+ B2B `organizationType`/`estimatedClientVolume`) into 7 broader categories: Overstay, Declared Undesirable, First Time Entry, Travel Assistance, Immigration Consultation, Professional Partnership, Enterprise Demo Request. No DB enum change — the mapping is read-side only. (2) New `components/help-tooltip.tsx` wrapping every leads-table column header with a white-bg, dark-text Radix tooltip explaining the field, plus a missing "Actions" header on the trailing column. (3) `PreferredCommunicationCell` now uses Radix HoverCard (interactive — Tooltip auto-closed on cursor-into-content) to expose the actual contact detail with mask-by-default + Reveal/Hide + copy-to-clipboard. (4) Pipeline drag-and-drop is bidirectional (see "Bidirectional Lead Pipeline" architecture decision above). (5) Table density bumped: `py-3` row padding, alternating `even:bg-muted/10`, hover row tint.
*   **Phase 5 Phase E — Draft Templates (§3.C):** New table `comm_templates` (`lib/db/src/schema/templates.ts`): id, name, category (text, 5 values: `promotional` / `system_update` / `new_feature` / `educational` / `customer_experience`), channel (text, `email` | `whatsapp`), subject (nullable, email-only), body, createdBy/updatedBy (uuid soft-refs to admin_users), createdAt/updatedAt, archivedAt (soft-delete). Backend: `routes/adminTemplates.ts` exposes list (with `?category=`, `?channel=`, `?includeArchived=true` filters) / create / get / patch / archive / unarchive / preview under `/api/admin/templates`, all behind `requireAdminAuth`. Email-channel invariant enforced on BOTH create and patch (subject required if channel=email). Channel is locked after create (not in PatchTemplateSchema) — operator must clone into a new template to switch channels. Archived templates are immutable (PATCH returns 409); archive is a soft-delete via `archivedAt = now()`. Preview renders against a fixed sample context (`Alex Mokoena` / `EMA-DEMO-0001` / `Acme Immigration Co`) and reuses `lib/campaignRender.ts` so the 4 supported tokens (`first_name`, `full_name`, `reference`, `organization_name`) are the SAME in templates as in campaigns — no divergence. Audit: every mutation writes a `comm_template_*` row via `void writeAudit({req, action, after})` (fire-and-forget pattern, matches adminCampaigns). Frontend: `TemplatesPanel` in `admin-communications.tsx` is a 1+2-col split (list left, editor right) with category filter chips, channel dropdown, "Show archived" checkbox, live client-side preview that mirrors the server's sample context (avoids per-keystroke roundtrip), and visual warning for unknown tokens. Campaign editor (`admin-campaign-editor.tsx`) gained a "Load from template" button that opens a channel-filtered picker dialog and replaces the campaign's subject+body with the template's; campaign name is only auto-promoted if it's still the default `"Untitled campaign"`. Audience filter and channel are intentionally NOT touched. **Known V1 limitation:** no version history; editing a template does not reach back into campaigns that previously loaded from it (the load is a one-shot copy).
*   **Phase 5 Phase D — Responsiveness pass (§12, no schema change):** Made every admin surface usable on phone/tablet without losing the desktop polish. (1) Kanban pipeline now drags on touch devices: added `drag-drop-touch` polyfill (~30kb, side-effect import in `lead-pipeline-board.tsx`), which transparently bridges the existing native HTML5 `draggable` attribute onto touchstart/move/end events. Zero API change in the board itself — the polyfill is a no-op on desktop mouse and self-installs on first import. (2) Wrapped 5 previously-unwrapped tables in `overflow-x-auto` containers so they scroll horizontally instead of bursting the viewport: `admin-users.tsx` admins table, `admin-communications.tsx` Campaigns + Reports tables, `admin-campaign-detail.tsx` recipients table, `admin-import.tsx` mapping table. (3) Fixed `admin-import.tsx` preview-stats grid (`grid-cols-3` → `grid-cols-1 sm:grid-cols-3`) so the three Stat tiles stack on phones instead of crushing into 90px-wide columns. (4) Communications-hub tab strip (`admin-communications.tsx`) gained `overflow-x-auto whitespace-nowrap` plus a negative-margin/padding bleed so it scrolls horizontally inside the page padding on <=375px viewports rather than wrapping or overflowing the viewport — the four tab labels (notably "System Notifications") otherwise busted the page width. AdminLayout's mobile sheet drawer (already shipped in Phase B) and the lead-mix Recharts (already wrapped in `ResponsiveContainer`) needed no further work. Open items intentionally deferred: the leads table on `admin.tsx` is the densest surface (10 columns) and already has its own `overflow-x-auto` wrapper; a longer-term redesign (column-priority hide / mobile card layout) is out of scope for the responsiveness pass and will land alongside the §3.C Templates work in Phase E if the brief calls for it.
*   **Phase 5 Phase C — Communications hub (no schema change):** The standalone `/admin/campaigns` surface is gone. It has been folded into a new `/admin/communications` shell with four tabs — **Campaigns** (the existing list, embedded as `CampaignsPanel`), **Templates** and **System Notifications** (V1 "coming soon" placeholders, intentional anchors so future work has a home), and **Reports** (DB-only aggregates). All four tabs render inside `pages/admin-communications.tsx` and pick the active tab from the URL (`/admin/communications`, `/admin/communications/templates`, `/admin/communications/notifications`, `/admin/communications/reports`). The campaign editor and detail pages now live at `/admin/communications/campaigns/:id/edit` and `/admin/communications/campaigns/:id`. Legacy `/admin/campaigns/*` paths are preserved as a `LegacyCampaignsRedirect` route in `App.tsx` that calls `setLocation(..., { replace: true })` so old bookmarks resolve cleanly without a back-button bounce. Sidebar nav was renamed Campaigns → Communications (icon kept). The Reports panel reads `GET /api/admin/campaigns/stats` (new endpoint in `routes/adminCampaigns.ts`), which returns purely DB-derived totals: campaigns by status (draft/sending/completed/cancelled), campaigns by channel (email/WhatsApp), recipient totals (sent/failed/skipped/unsubscribed), and a 10-row recent-activity feed. Open-rate and click-rate metrics are intentionally NOT reported — they require provider webhooks (Resend events / WhatsApp delivery receipts) which are not wired yet. The Reports panel includes a top-line note documenting that gap so operators don't assume the absence implies zero. The old `pages/admin-campaigns.tsx` file was deleted.
*   **Phase 5 Phase B — Admin chrome refactor (no schema change):** Introduced `components/admin-layout.tsx` — a single shell that renders a sticky topbar (page title + per-page `actions` slot + `AdminUserMenu`), a desktop left sidebar (`w-60`, 5 nav items: Dashboard / Campaigns / Imports / Manage Admins [superadmin-only] / Profile, lucide icons, active state via `useLocation` `startsWith`), and a mobile Sheet drawer toggled by a hamburger button. All 9 admin pages (`admin`, `admin-campaigns`, `admin-campaign-editor`, `admin-campaign-detail`, `admin-users`, `admin-profile`, `admin-import`, `admin-lead-detail`, `admin-case-detail`) were migrated off the per-page `<BrandHeader variant="compact" rightSlot={<AdminUserMenu/>}/>` chrome and now render inside `<AdminLayout>`. Pages that need a custom background or content container (campaign editor/detail keep their dark slate gradient; lead/case detail keep their narrower max-w) pass `bodyClassName` / `contentClassName` overrides. Per-page page-level action buttons (Refresh / Export CSV / Import Leads on the dashboard, etc.) are passed via the `actions` prop and additionally mirrored inside the page body on mobile because the topbar collapses on small screens. `BrandHeader` and `AdminUserMenu` imports have been removed from every admin page.
*   **Phase 1 Frontend Polish (Q1=C, Q2=X):** The reference number is generated server-side at lead-insert (end of Terms / step 6) and stored in `createdLead.referenceNumber`, but is intentionally NOT rendered anywhere inside the assessment flow. After `finalize` succeeds, `finalizeAndShowSummary` redirects to `/thank-you/:reference` — the single confirmation surface that reveals the reference and personalised note. The in-page Summary block in `assessment.tsx` is left in place as a defensive fallback only (triggered when `referenceNumber` is somehow missing); do not re-introduce a Summary-step reference banner. The DocumentUploader frontend MIME/extension allow-list has been removed (`accept="*/*"`) with a soft "PDF/JPG/PNG/DOC/DOCX processed fastest" hint; the server's allow-list in `routes/documents.ts` remains the source of truth and surfaces rejection messages in the inline error banner. Personalised note (`lib/personalisedNote.ts`) gained explicit cross-border, passport-risk, overstay-appeal/review, and document-prep guidance. Step-2 passport question reworded to "Is your passport valid for at least 6 months from today?".

## Pointers

*   _Populate as you build_